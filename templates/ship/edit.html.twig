{% extends 'base.html.twig' %}

{% block title %}Edit Ship{% endblock %}

{% block body %}
    <h1>Edit Ship</h1>

    {{ include('ship/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_ship_index') }}">back to list</a>

    {{ include('ship/_delete_form.html.twig') }}
{% endblock %}
